<Page xmlns="http://schemas.nativescript.org/tns.xsd">
    <ListView items="{{ todos }}" itemTap="{{ onItemTap }}" class="list-group">
        <ListView.itemTemplate>
            <GridLayout columns="*, auto" class="list-group-item p-2">
                <Label text="{{ title }}" textDecoration="{{ completed ? 'line-through' : 'none' }}" 
                       class="{{ completed ? 'text-gray-400' : 'text-gray-800' }}" col="0" />
                <Label text="✓" class="text-green-500 text-lg" col="1" 
                       visibility="{{ completed ? 'visible' : 'collapsed' }}" />
            </GridLayout>
        </ListView.itemTemplate>
    </ListView>
</Page>